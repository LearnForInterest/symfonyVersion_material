{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Orderform list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Need_num</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('orderform_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.neednum }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('orderform_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('orderform_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
